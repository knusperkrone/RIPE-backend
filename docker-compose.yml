version: "2"
services:
  db:
    restart: "always"
    image: postgres:13
    # volumes: ./data:/var/lib/postgresql/data
    environment:
      POSTGRES_DB: iftem
      POSTGRES_USER: iflab
      POSTGRES_PASSWORD: Tischfussball

  db_schema:
    restart: "no"
    image: clux/diesel-cli
    volumes:
      - "./:/volume"
    working_dir: "/volume"
    environment:
      DATABASE_URL: postgres://iflab:Tischfussball@db:5432/iftem
    depends_on:
      - db
    entrypoint: ["sh", "-c", "sleep 2 && diesel migration run"]

  threshold_plugin:
    image: docker-registry.if-lab.de/arme/iftem_backend/treshold_plugin/master
    volumes:
      - ./plugins:/out

  backend:
    restart: "always"
    image: docker-registry.if-lab.de/arme/iftem_backend
    environment:
      BIND_ADDR: 0.0.0.0:8000
      DATABASE_URL: postgres://iflab:Tischfussball@db:5432/iftem
      MQTT_PORT: 1883
      MQTT_NAME: iftem-master
      MQTT_URL: 18.196.113.35
      PLUGIN_DIR: /plugins
    volumes:
      - ./plugins:/plugins
    ports:
      - 8000:8000
    depends_on:
      - db_schema
      - threshold_plugin
